# Leave empty during Vite dev to use same-origin proxy to the API.
# Set to full API origin when UI is hosted separately, e.g. https://api.example.com
VITE_API_URL=

# Optional: override dev proxy target (default http://localhost:3001)
# VITE_PROXY_TARGET=http://localhost:3001
